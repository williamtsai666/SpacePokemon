<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/space_craft_card_height"
    android:layout_margin="5dp"
    android:background="@drawable/shape_craft_bg">

    <ImageView
        android:id="@+id/iv_craft"
        android:layout_width="@dimen/craft_size"
        android:layout_height="@dimen/craft_size"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_spaceship"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_craft_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:padding="5dp"
        android:textColor="@color/white"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_craft"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Craft Name" />


</androidx.constraintlayout.widget.ConstraintLayout>